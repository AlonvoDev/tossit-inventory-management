services:
  - type: web
    name: tossit-app
    env: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npx serve -s dist -l tcp://0.0.0.0:$PORT
    envVars:
      - key: NODE_VERSION
        value: 20